<%= stylesheet_link_tag "userform" %>

<div class="prompt-container">
  <h1 id="prompt-title">User Login</h1>

  <div id="form-box">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field">
        <%= f.label :email, id:"form-title" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"entry-box" %>
      </div>
      
      <div class="field">
        <%= f.label :password, id:"form-title" %><br />
        <%= f.password_field :password, autocomplete: "current-password", class:"entry-box" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me, id:"form-title" %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "Log in", id:"sign-in-button" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
  
</div>